---
description: List curated external public skill repositories to consider.
---

Show Quartermaster's curated candidate sources:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/bin/qm" sources
```

These are suggestions only. External repositories are untrusted until scanned
with `qm intake <local checkout> --dry-run`.
